<UserControl
  xmlns="https://github.com/avaloniaui"
  xmlns:pads="clr-namespace:Restofus.Pads;assembly=Restofus"
  Padding="1"
  Background="#8e9bbc">
  
  <Grid>

    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="28" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#4d6082">
      <TextBlock Foreground="#ffffff" Margin="3" Text="{Binding I18N[Response]}" />
    </StackPanel>

    <Rectangle Grid.Row="1" Fill="#cfd6e5"/> 
    
    <pads:ResponseViewer Grid.Row="2" DataContext="{Binding ResponseViewerContext}" />
        
  </Grid>
  
</UserControl>